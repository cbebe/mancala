name: "Move"
description: "Move the stones across the board"
inputs:
  title:
    description: "Github issue title"
    required: true
  user:
    description: "Github user"
    required: true
runs:
  using: "node12"
  main: "dist/main.js"
